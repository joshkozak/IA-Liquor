# Create a new requirements.txt with all required packages
echo streamlit>=1.28.0 > requirements.txt
echo pandas >> requirements.txt
echo folium >> requirements.txt
echo plotly >> requirements.txt
echo plotly-express >> requirements.txt
echo numpy >> requirements.txt
echo scikit-learn >> requirements.txt
echo branca >> requirements.txt

# Commit and push the updated requirements
git add requirements.txt
git commit -m "Update requirements.txt with all dependencies"
git push origin main